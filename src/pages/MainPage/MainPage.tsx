import React from 'react';
import {Counter} from "../../components/counter/counter";

const MainPage = () => {
    return (
        <div>
            <Counter />
        </div>
    );
};

export default MainPage;
